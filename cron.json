{
  "jobs": [
    {
      "command": "30 7 * * * npm run cli:transformation -- -a transform -f jobteaser",
      "size": "L"
    },
    {
      "command": "45 7 * * * npm run cli:load -- -a load -f jobteaser",
      "size": "L"
    }
  ]
}
