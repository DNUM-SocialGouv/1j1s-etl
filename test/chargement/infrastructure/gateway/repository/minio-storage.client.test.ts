import { expect } from "@test/configuration";

describe("MinioStorageClientTest", () => {
	context("Structure de test, à remplacer", () => {
		beforeEach(() => {
			expect(true).to.be.true;
		});

		it("Test faux à défaut", () => {
			expect(false).to.be.true;
		})
	})
});
