let fileName: string;

describe("Test Cron File", () => {
	beforeEach(() => {
		fileName = "wttj";
	});

	context("lorsque minio est vide", () => {
		it("echoue", () => {
			// Given

			// When

			// Then
		});
	});

	context("lorsque minio est plein", () => {
		beforeEach(() => {

		});

		it("copie le fichier en local", () => {

		});

		it("nomme le fichier en fonction de la date courante", () => {

		});
	});
});
